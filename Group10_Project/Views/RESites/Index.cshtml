@model IEnumerable<Group10_Project.Models.RESite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.siteAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.siteContactPerson)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RE.contactPersonName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.siteAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.siteContactPerson)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RE.contactPersonName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
